/**
 * GENERATED FILE - do not edit by hand.
 *
 * Written by `pnpm fetch:unsplash`, which reads UNSPLASH_ACCESS_KEY from the
 * environment, resolves each slot in scripts/fetch-unsplash.mjs, and triggers
 * the download endpoint that the Unsplash API guidelines require on use.
 *
 * Empty until that script is run. Every consumer treats a missing slot as
 * "no photo yet" and falls back, so the site renders correctly either way.
 */
import type { UnsplashPhoto } from "@/lib/unsplash";

export const unsplashPhotos: Record<string, UnsplashPhoto> = {};
